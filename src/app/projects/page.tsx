import { PageIntro } from "@/components/page-intro";

export default function ProjectsPage() {
  return (
    <section aria-labelledby="projects-title" className="max-w-3xl">
      <PageIntro
        label="Projects"
        title="Projects"
        description="Selected work and project details will be added here later."
        titleId="projects-title"
      />
    </section>
  );
}
